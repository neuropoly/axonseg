% diam_mean=mean(cat(1,axonlist(indexes{i}).axonEquivDiameter));
    diam_std=std(cat(1,axonlist(indexes{i}).axonEquivDiameter));
        gRatio=mean(cat(1,axonlist(indexes{i}).gRatio));
        
        
                
diam_mean=mean(cat(1,axonlist.axonEquivDiameter));
    diam_std=std(cat(1,axonlist.axonEquivDiameter));
        gRatio=mean(cat(1,axonlist.gRatio));
        
        max_diam=max(cat(1,axonlist.axonEquivDiameter));
        max_gRatio=max(cat(1,axonlist.gRatio));